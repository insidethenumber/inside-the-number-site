# Weekly growth report — Monday, September 14, 2026

**This week's pull failed. Every data source below is "could not read" — not zero, not flat, unread.**

Root cause: this run is unattended (scheduled task, no one present), and every access path required a live approval that never came:
- Beehiiv MCP (`list_publications`) — auto-declined twice: "no one was available to approve it during this scheduled run"
- Browser access to app.beehiiv.com and dash.cloudflare.com — declined the same way when requested
- Direct API calls from the sandbox to api.x.com — blocked (network not allowlisted for that host; github.com works fine, x.com does not)
- Google Search Console — no API credentials exist anywhere in the repo (confirmed by search); dashboard-only, and browser access has the same approval block as above
- Reddit — no API credentials exist (`REDDIT_CLIENT_ID`/`SECRET` are documented as not-yet-set-up in `ACCESS-SETUP.md`)
- Instagram — Graph API token IS present in `itn-secrets.env` now (wasn't as of Sep 13), but nobody wrote the pull script yet, and there was no time left in this run to write and test one safely

```
SUBSCRIBERS: could not read (was 3 as of Sep 13) | sources: could not read
TRAFFIC: could not read (was ~106/week as of Sep 10)
SEARCH: could not read (was 0 clicks / 793 impr / pos 70.6 as of Sep 10, 28-day)
X: could not read (was ~48 followers off ~500 posts as of Sep 13)
INSTAGRAM: could not read (was 2 followers / 4 posts as of Sep 13)
REDDIT: could not read
RECOMMENDATIONS: could not read (was 0 in / 0 out as of Sep 13 — flag this every week until it changes)

WHAT MOVED: unknown — no data was pulled this week. Do not assume "nothing moved"; that's a claim this run can't back up.
WHERE THE WEEK WENT: unknown — this report can't see subscriber or traffic activity from the past 7 days.
NEXT WEEK, ONE THING: fix the access layer before anything else. This report is worthless if it can't read its own numbers. Two concrete fixes, in order: (1) either mark the Beehiiv MCP and the Cloudflare/GSC browser sites as standing-approved for scheduled runs, or have a script pull them with the API tokens that already exist in itn-secrets.env (Cloudflare token is set; Beehiiv would need a direct API key per ACCESS-SETUP.md §4, since the connector needs a live approval this run doesn't have). (2) Write and test an Instagram Graph API pull script — the token exists now, unlike last week. Cost: under an hour of Chuck's/Claude's time in an interactive session; zero dollars.
NEEDS CHUCK: approve the Beehiiv connector (and Cloudflare/GSC site access, if browser-based) for scheduled/unattended runs, or add a Beehiiv API key to itn-secrets.env so this doesn't depend on live approval. Also: Reddit API credentials were never finished (REDDIT_CLIENT_ID/SECRET), so u/kceza1's activity has never been pulled automatically — that's a decision for Chuck on whether it's worth the 15 minutes.
```

*Filed by the automated Monday growth report. This is a diagnostic failure report, not a growth report — treat it as a punch list, not a status update.*
