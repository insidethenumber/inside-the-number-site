# Session — Saturday, September 12, 2026

## Newsletter run (itn-daily-weekend, 6:15 AM CT start)

Built and scheduled the Morning Board for 7:45 AM CT. Worked from
DRAFT_2026-09-12.md (pre-staged Friday evening) rather than a full board
re-survey; re-verified the three named games plus the biggest move and the
number-to-know against live ESPN odds before building.

Card: free pick Under 49.5 (Ohio State at Texas), also-plays Over 48.5
(Arizona at BYU) and Under 9 (Pirates at Cubs). Two of the three draft
numbers had drifted since Friday evening — Arizona/BYU's price and the
Pirates/Cubs total's price-vs-movement claim — both picks were flipped to
match the true-price math on the fresh pull rather than publishing the
stale draft numbers. Full detail in SEND_LOG.md.

Site (index.html, cfb.html) and newsletter assets (header/pick/move/number
images + issue.html) pushed and confirmed live before opening Beehiiv.
Issue built via the documented ClipboardEvent-paste method; the first paste
attempt onto the duplicated post left the old issue's header image and body
in place underneath the new content (both Cmd+A and execCommand('selectAll')
failed to clear atom image nodes reliably). Fixed by looping a DOM-Range
`selectNodeContents` + `execCommand('delete')` three times until the editor
was provably empty, then pasting once — produced a clean 4-image, beehiiv-
hosted body with no leftovers. Worth trying that Range-loop first on future
runs instead of the keyboard shortcuts.

Scheduled for Sat, Sep 12, 2026 7:45 AM CDT, confirmed via the modal echo and
a post-reload check on /posts. Thumbnail upload didn't take (left blank —
lower risk than a wrong-dated one). DRAFT_2026-09-12.md deleted in the same
push that shipped the issue.
