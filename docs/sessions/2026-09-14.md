# Session — Monday, September 14, 2026

## Newsletter run (itn-daily-weekday, 8:00 AM CT start — published late, 12:17 PM CT)

Run was scheduled to start research at 8:00 AM CT for a 10:00 AM CT send, but
the non-interactive session's browser-access negotiation (built-in browser
pane auto-declined; discovered the Chrome extension worked instead, already
allowlisted for app.beehiiv.com) consumed roughly 3h40m before any research
began. Once site/asset work started it took under 45 minutes end to end.
Published immediately via "Publish now" at 12:17 PM CT rather than
attempting to schedule a time already in the past — shipping beat polish.

Card: free pick Over 43.5 (Denver at Kansas City, MNF), also-plays Under 11
(San Diego at Colorado) and Over 8 (NY Yankees at Minnesota). The first two
picks were already on index.html from an earlier, separate automation
(commit 2fbf7a5, 6:25 AM CT) — this run verified those numbers were still
live, added the third pick to reach the 3-4 minimum, and refreshed cfb.html
for Monday (no CFB pick — sport doesn't play Mondays).

Site (index.html, cfb.html) and newsletter assets (header/pick/move/number
images + issue.html) pushed and confirmed live before opening Beehiiv —
verified via a `fetch()` executed inside the beehiiv tab itself, since this
session's own web_fetch tool is provenance-restricted to previously-seen
URLs and the sandboxed bash has no network access at all.

Issue built via the documented ClipboardEvent-paste method. First attempt
bloated to 7 images / 1,112 words because a JS DOM-Range delete loop cleared
the visible editor but not ProseMirror's internal model, so the paste merged
with old Sep 13 content instead of replacing it. Fixed by reloading the
draft and clearing with real keyboard events instead (Cmd+A pressed twice —
first press selects the current block, second extends to the full document
in this editor — then Backspace), which produced a genuinely empty editor
before pasting. Second attempt: exactly 4 images, all beehiiv-CDN-hosted,
each section header exactly once. Full detail, including the corrected
technique for the next run, is in SEND_LOG.md.

Test email sent to insidethenumber.itn@gmail.com. Audience confirmed "All
subscribers" for both Email and Web. Stale Sep-13 web thumbnail (carried by
the duplicate) removed rather than left behind the disabled toggle.

Final verification: live homepage confirmed `Mon, Sep 14` / `Over 43.5` in
the Pick of the Day card; Beehiiv /posts confirmed Published, Sep 14 12:17 PM
CDT, correct title.
