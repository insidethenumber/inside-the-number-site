# 2026-09-07 (Monday, Labor Day) — itn-daily-weekend scheduled run

## Newsletter run

Ran the holiday-gated weekend routine (day-of-week is Monday, but 2026-09-07
is on the holiday list, so `itn-daily-weekend` fired and `itn-daily-weekday`
correctly sat this one out). Full details, audit counts and picks are in
`SEND_LOG.md`'s 2026-09-07 row — summary here for the session index.

- Surveyed the full board live via ESPN (sandbox 403s persist, read through a
  browser tab): 11 MLB games, 1 CFB game (SMU @ Florida State, Labor Day
  night), 0 NFL/NBA/CBB/NHL/UFC, 0 PGA (next event Sep 17-20).
- Card: SMU -3 (free pick, lead per football-first), Under 8 (Guardians @
  Orioles), Milwaukee -1.5 (Cubs @ Brewers). 0 moneylines of 3. Biggest
  overnight move (not a pick): Twins/Tigers run-line flip.
- Site (`index.html`) `todaysGames` and Pick of the Day card updated;
  `cfb.html` PICK block correctly left alone (that step is Saturday-only).
- Newsletter images: header/pick/move/number all generated via
  `scripts/newsletter_assets.py`, pushed, confirmed 200 on
  insidethenumber.com, and confirmed beehiiv-hosted (GATE 2) inside the post.
- **Finding worth keeping:** after the documented ClipboardEvent paste, the
  editor left the ENTIRE previous issue's body appended after the new
  content, and separately produced a duplicate header image. Fixing both
  with raw DOM `node.remove()` looked correct on screen and passed checks in
  the moment, but did not survive a page reload — beehiiv's save cycle
  doesn't appear to pick up plain DOM mutations. What did survive a reload:
  editing the real Tiptap document via
  `document.querySelector('.tiptap.ProseMirror').editor.getJSON()`, mutating
  the JSON (drop the bad node, set `alt` text), and writing it back with
  `editor.commands.setContent(json, true)`. Re-verified clean after a hard
  reload. Recommend this method over raw DOM edits in the task prompts for
  any future post-paste cleanup.
- Scheduled (not published) for **Sep 7, 2026 7:45 AM CDT**, verified on
  `/posts` showing status Scheduled. `itn-deadline-check-weekend` (7:10 /
  8:10 AM CT) is the safety net that verifies the actual send.
